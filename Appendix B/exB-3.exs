# B.2.4 익명 함수

(... -> integer)		# 임의의 파라미터를 받아 정수를 반환
(list(integer) -> integer)	# 정수 리스트를 받아 정수를 반환
(() -> String.t)		# 파라미터 없이 문자열을 반환
(integer, atom -> list(atom))	# 정수와 아톰을 받아 리스트를 반환


(atom, float -> list)
((atom, float) -> list)
(list(integer) -> integer)
((list(integer)) -> integer)
